name: Sync script data

on:
  schedule:
    # every 6h UTC; skips early when both upstreams are unchanged and models match MODEL_VERSION
    - cron: '13 */6 * * *'
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      force:
        description: Re-parse even if upstream is unchanged
        type: boolean
        default: false

concurrency:
  group: sync-data
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  keepalive:
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: Keep this schedule enabled
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh api -X PUT "repos/${{ github.repository }}/actions/workflows/build.yml/enable"

  sync:
    if: github.event_name != 'push' || !startsWith(github.event.head_commit.message, 'Update DayZ release data')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Sync DayZ Wiki release notes
        id: wiki
        continue-on-error: true
        run: npm run sync:release-notes

      - name: Check upstream for changes
        id: upstream
        run: |
          HEAD=$(git ls-remote https://github.com/BohemiaInteractive/DayZ-Script-Diff.git refs/heads/main | cut -f1)
          echo "head=$HEAD" >> "$GITHUB_OUTPUT"
          echo "Upstream head: $HEAD"
          if [[ -f data/versions.json ]]; then
            COMMITTED=$(node -p "require('./data/versions.json').upstreamHead")
            echo "committed=$COMMITTED" >> "$GITHUB_OUTPUT"
            echo "Committed head: $COMMITTED"
          else
            echo "committed=" >> "$GITHUB_OUTPUT"
            echo "No committed versions.json"
          fi

          EXP_HEAD=$(git ls-remote https://github.com/BohemiaInteractive/DayZ-Script-Diff-Experimental.git refs/heads/main | cut -f1)
          echo "exp_head=$EXP_HEAD" >> "$GITHUB_OUTPUT"
          echo "Experimental head: $EXP_HEAD"
          if [[ -f data/experimental.json ]]; then
            EXP_COMMITTED=$(node -p "require('./data/experimental.json').sha||''")
            echo "exp_committed=$EXP_COMMITTED" >> "$GITHUB_OUTPUT"
            echo "Committed experimental: $EXP_COMMITTED"
          else
            echo "exp_committed=" >> "$GITHUB_OUTPUT"
            echo "No committed experimental.json"
          fi

          # Heal MODEL_VERSION bumps that only rewrote the tip model (or missing files).
          STALE=$(node -e '
            const fs = require("fs");
            const path = require("path");
            const src = fs.readFileSync("src/parse-all.js", "utf8");
            const expected = Number(src.match(/MODEL_VERSION\s*=\s*(\d+)/)[1]);
            const data = fs.existsSync("data/versions.json")
              ? require("./data/versions.json")
              : { versions: [] };
            const versions = [...(data.versions || [])];
            if (data.experimental) versions.push(data.experimental);
            let stale = 0;
            for (const v of versions) {
              const name = v.channel === "experimental" ? "experimental" : v.build;
              const file = path.join("data", `model-${name}.json`);
              if (!fs.existsSync(file)) { stale++; continue; }
              const buf = Buffer.alloc(64);
              const fd = fs.openSync(file, "r");
              fs.readSync(fd, buf, 0, 64, 0);
              fs.closeSync(fd);
              const m = buf.toString("utf8").match(/"modelVersion"\s*:\s*(\d+)/);
              if (!m || Number(m[1]) !== expected) stale++;
            }
            console.log(stale);
          ')
          echo "stale=$STALE" >> "$GITHUB_OUTPUT"
          echo "Stale/missing models: $STALE (MODEL_VERSION from parse-all.js)"

      - name: Decide whether to sync
        id: decide
        run: |
          SYNC_STABLE=false
          SYNC_EXP=false
          if [[ "${{ github.event_name }}" != "schedule" || "${{ inputs.force }}" == "true" || "${{ steps.upstream.outputs.head }}" != "${{ steps.upstream.outputs.committed }}" || "${{ steps.upstream.outputs.stale }}" != "0" ]]; then
            SYNC_STABLE=true
          fi
          if [[ "${{ github.event_name }}" != "schedule" || "${{ inputs.force }}" == "true" || "${{ steps.upstream.outputs.exp_head }}" != "${{ steps.upstream.outputs.exp_committed }}" ]]; then
            SYNC_EXP=true
          fi
          echo "sync_stable=$SYNC_STABLE" >> "$GITHUB_OUTPUT"
          echo "sync_exp=$SYNC_EXP" >> "$GITHUB_OUTPUT"
          # fetch + parse when either repo moved: experimental lives in versions.json
          # and needs a model, so a new experimental head still runs the stable path.
          if [[ "$SYNC_STABLE" == "true" || "$SYNC_EXP" == "true" ]]; then
            echo "sync=true" >> "$GITHUB_OUTPUT"
            echo "sync_fetch=true" >> "$GITHUB_OUTPUT"
          else
            echo "Upstream unchanged and models current - skipping."
            echo "sync=false" >> "$GITHUB_OUTPUT"
            echo "sync_fetch=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Restore upstream clone
        if: steps.decide.outputs.sync == 'true'
        uses: actions/cache@v4
        with:
          path: .cache
          key: upstream-${{ steps.upstream.outputs.head }}-${{ steps.upstream.outputs.exp_head }}
          restore-keys: |
            upstream-

      - name: Test parser
        if: steps.decide.outputs.sync_fetch == 'true'
        run: npm test

      - name: Fetch upstream versions
        if: steps.decide.outputs.sync_fetch == 'true'
        run: npm run fetch

      - name: Parse all versions
        if: steps.decide.outputs.sync_fetch == 'true'
        env:
          FORCE_PARSE: ${{ inputs.force == true && '1' || '' }}
        run: |
          if [[ -z "$FORCE_PARSE" && "${{ github.event_name }}" == "push" && "${{ github.event.before }}" != "0000000000000000000000000000000000000000" ]]; then
            if git diff --name-only "${{ github.event.before }}" "${{ github.sha }}" | grep -qE '^(src/parser/|src/parse-all\.js$)'; then
              export FORCE_PARSE=1
            fi
          fi
          npm run parse

      - name: Sync experimental snapshot
        if: steps.decide.outputs.sync_exp == 'true'
        run: npm run experimental

      - name: Commit data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data
          if git diff --staged --quiet; then
            echo "No data changes to commit."
            exit 0
          fi
          git commit -m "Update DayZ release data" -m "Script upstream ${{ steps.upstream.outputs.head }}; experimental ${{ steps.upstream.outputs.exp_head }}"
          git push

      - name: Summary
        if: always()
        run: |
          {
            echo "### Sync script data"
            echo "- Event: \`${{ github.event_name }}\`"
            echo "- Wiki notes: \`${{ steps.wiki.outcome }}\`"
            echo "- Upstream: \`${{ steps.upstream.outputs.head }}\`"
            echo "- In repo: \`${{ steps.upstream.outputs.committed }}\`"
            echo "- Experimental: \`${{ steps.upstream.outputs.exp_head }}\`"
            echo "- Experimental in repo: \`${{ steps.upstream.outputs.exp_committed }}\`"
            echo "- Stale/missing models: \`${{ steps.upstream.outputs.stale }}\`"
            echo "- Sync stable: \`${{ steps.decide.outputs.sync_stable }}\`"
            echo "- Sync experimental: \`${{ steps.decide.outputs.sync_exp }}\`"
          } >> "$GITHUB_STEP_SUMMARY"
