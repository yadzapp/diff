# Open-source DayZ mods for Compare testing
#
# Shallow clones from GitHub. Gitignored — not part of the site build.
# Drop a repo folder (or an Addons/ subfolder with scripts) on /compare/.
#
# Prefer ones with many overrides: Community Framework, VPP, COT, RevivalPlus,
# metricz, HeroesAndBandits, MoreExplosives, dayz-mod-carim, MuchCarKey.
